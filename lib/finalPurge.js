// this takes care of the final build and the minified production ready css file;
//uses the same config used in building initial css to build the final one.

//gets the filepath for the final build from path checker, 
//purges the files and drops the final build in a folder